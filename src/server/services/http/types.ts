export enum HttpMethods {
  GET = `get`,
  POST = `post`,
  PUT = `put`,
  DELETE = `delete`,
  CONNECT = `connect`,
  LINK = `link`,
  UNLINK = `unlink`,
  PATCH = `patch`,
  OPTIONS = `options`,
  HEAD = `head`,
}
